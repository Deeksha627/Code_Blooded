function selectYear(year) {
  alert(`You selected the year ${year}.`);
  // Perform further actions here, such as redirecting to a resource page or downloading files.
  // Example: window.open(`https://example.com/resources/${year}.pdf`, '_blank');
}
